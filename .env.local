# No client-side keys needed.
# The Fireworks AI API key is configured as a Convex environment variable:
#    npx convex env set FIREWORKS_API_KEY fw_xxxxxxxxx
# This keeps the key server-side — never shipped to the browser.